<%#
# paramètres header

*

%>

<header class="<%= prefix %>-header">
    <div class="<%= prefix %>-container%>" >
        <%- /* include('./head', locals.head); */ %>

        <div class="<%= prefix %>-header__body">

            <%- include('./brand', locals.brand); %>

            <% if (locals.identity !== undefined) { %><%- include('./identity', identity); %><% } %>

            <% if (locals.tools !== undefined) { %><%- include('./tools', tools); %><% } %>
        </div>

        <%
        switch(true) {
        case locals.navigationTpl !== undefined: %>
        <%- include(navigationTpl); %>
        <% break;

        case locals.navigation !== undefined: %>
        <%- include( '../../../navigation/tests/templates/navigation', navigation); %>
        <% break;
        }
        %>

    </div>
</header>