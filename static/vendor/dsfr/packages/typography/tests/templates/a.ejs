<%
let attrs = locals.attrs || {};

attrs.title = locals.title;
attrs.href = locals.href;

if (locals.blank === true) {
    attrs.target= '_blank';
    attrs.rel= 'noopener';
}
else attrs.target = '_self';

%>

<a class="<%= prefix %>-link" <%- includeAttrs(attrs); %>><%- label%></a>